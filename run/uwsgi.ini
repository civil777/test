[uwsgi]
uid = jake
base = /home/jake/testhomepage

chdir = %(base)
module = config.wsgi:application
env = DJANGO_SETTINGS_MODULE=config.settings

master = true
processes = 2

socket = %(base)/run/uwsgi.sock
chown-socket = jake:www-data
chmod-socket = 660
vacuum = true


